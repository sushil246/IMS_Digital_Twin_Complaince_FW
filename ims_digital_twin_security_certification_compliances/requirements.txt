# Core Google ADK + LiteLLM (Ollama backend)
google-adk>=1.0.0
litellm>=1.40.0
google-generativeai>=0.8.0

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-multipart>=0.0.9

# Utilities
pydantic>=2.6.0
aiofiles>=23.2.1

# Optional: if running alongside the parent ims_digital_twin project
# (no additional install needed — uses relative imports)
